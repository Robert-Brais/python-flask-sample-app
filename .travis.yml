sudo: required

language: python

python:
- "2.7"

services:
- docker

before_install:
-docker build . -t minitwit
-docker run -p 5000:5000 minitwit

script:
-docker run minitwit python setup.py test
